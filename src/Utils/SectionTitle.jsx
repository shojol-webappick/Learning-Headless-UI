// eslint-disable-next-line react/prop-types
const SectionTitle = ({ title }) => {
  
  return (
    <div>
      <h2 className="text-center text-2xl font-bold text-blue-600 my-12">{title}</h2>
    </div>
  );
};

export default SectionTitle;
